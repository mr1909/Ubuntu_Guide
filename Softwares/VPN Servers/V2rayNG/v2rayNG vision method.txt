v2rayNG vision method:

https://github.com/reeceyng/v2ray-agent




apt-get update -y && apt-get upgrade -y

-- or

apt update && apt upgrade -y

-----------------------------

wget -P /root -N --no-check-certificate "https://raw.githubusercontent.com/reeceyng/v2ray-agent/master/install.sh" && chmod 700 /root/install.sh && /root/install.sh


-- 1 [install] >> 3 [vision] >> sw.domain.com [cloudflare] >> 1 [let's encrypt]

-----------------------------

-- for all other questions just press enter.